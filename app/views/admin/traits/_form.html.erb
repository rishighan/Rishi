<%= form_for([:admin,@trait], :html=>{:class=>"span12"}) do |f| %>
  <% if @trait.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trait.errors.count, "error") %> prohibited this attribute from being saved:</h2>

      <ul>
      <% @trait.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 
  <div class="well">
    <legend><%=controller.action_name.capitalize%> Trait </legend>

    <%= f.label  :name, "Trait Name" %>
    <%= f.text_field :name %>
    <br><br>

    <%= f.label :description, "Description" %>
    <%= f.text_area :description, :rows=>5, :class=>"span8" %>
  
    <br><br>
    <%= f.submit :class=>"btn-primary" %>
  </div>
<% end %>
